# 量化系统当前买卖逻辑

本文描述的是当前已经实现和正在收敛的逻辑。系统已经支持基于本地 5 分钟 K 线的分时回放，但还没有接入盘口快照、逐笔成交和真实委托撮合。

## v0.2.135 研究重任务不属于日常交易逻辑

- 日常买卖逻辑继续使用已训练、已复盘或已落库的策略运行结果；训练、策略进化、策略复盘和时间线回测只由后台手动触发或显式维护命令运行。
- `QT_RESEARCH_TASKS_MANUAL_ONLY=true`、`QT_TIMELINE_REQUIRE_MANUAL_TRIGGER=true` 和前台预计算默认手动模式只改变触发边界，不改变买入评分、卖出评分、仓位、成交或账户派生规则。
- 前台推荐和日计划预计算默认只作为人工预热缓存工具，默认跳过未过期缓存、限制用户数、限制天数并限制单次运行时间；普通用户访问未命中时不再自动启动补算。

## 1. 数据输入

系统共用一套数据底座：

- `backend/data/quant_data.sqlite3`：新闻、AI 分析、日 K、分钟 K、龙虎榜、策略模型、训练轨迹的主存储。
- `backend/data/news_history.json`、`news_analysis_records.json`：历史兼容和迁移来源。
- `backend/data/biying_stock_list.json`：股票代码和名称映射。
- `backend/data/kline_day_cache/*.json`、`kline_cache/*.csv`：旧 K 线缓存，只作为兼容读取和迁移来源。

当前没有真实账户接口，不会真实下单；所有账户都是纸面撮合账户。

## 2. 产品账户模型

正确的产品模型是“共用数据、多个策略、用户独立跟随”：

- 数据服务持续抓新闻、行情、AI 分析、龙虎榜，并补齐缺失数据。
- 训练服务用同一份数据生产多个策略模型，每个模型有自己的基础参数、收益、回撤、胜率、成交、交割单和训练淘汰记录。
- 用户注册后选择模拟资金和一个跟随策略；账户从注册日或切换策略日开始，用该策略独立运行。
- 用户只看到自己跟随策略对应的持仓、成交、交割单和资金流水。
- 系统默认基础参数只用于人工调参、默认计算、生成新策略和诊断；它不是产品主账户，也不代表任何用户正在跟随。

因此，后台“持仓成交”应该按策略查看；策略库里的每个模型才是用户可跟随的策略；前台用户账户等于“用户资金 + 用户跟随策略 + 跟随开始日期”。

用户跟随信息保存在前台账号 profile 中：

- `simulated_cash`：用户模拟资金。
- `strategy_model_id`：用户当前跟随策略。
- `follow_started_at` / `follow_start_date`：本次跟随开始时间。新注册时取注册时间；切换策略或调整模拟资金时重置。

v0.2.32 起，前台账户派生结果会按用户、策略、模拟资金、跟随开始日和 `as_of` 写入 `user_follow_snapshots`、`user_follow_positions`、`user_follow_trades`。这些表是用户账户视图的物化结果；策略级别的公共运行结果仍保存在 `strategy_runtime_*` 表中。
v0.2.33 起，`user_follow_periods` 记录用户注册、设置资金或切换策略形成的跟随周期，后台用户管理页会展示当前周期、账户快照、持仓和最近成交来源。
当前阶段不做“每个用户从注册日开始重建完整独立策略运行”的重计算版本；前台先按用户资金和跟随开始日读取/裁剪策略级运行结果，真正的用户独立重跑保留在后续计划中。

## 3. 策略模型

策略分三类：

- 策略库模型：进化/回测后保存的参数组合，是主要可跟随对象。
- 资金档策略：1万、2万-5万、5万-10万、10万以上等资金约束预设，控制最大持仓、单票资金和可买一手能力。
- 系统默认基础参数：后台手动参数或拟合结果，只用于诊断、生成新策略和作为人工调参模板，不直接代表产品账户。

策略库模型只有被用户选择跟随后，才影响该用户账户。每个策略模型都必须保存自己的基础参数；后台把某个模型“复制为系统默认基础参数”不会改变任何用户已经选择的跟随策略。

## 4. 新闻事件结构化

每条新闻或 AI 分析记录会被转成 `NewsEvent`：

- `event_type`：政策催化、业绩财报、订单合作、产品技术、板块异动、宏观市场、风险事件等。
- `industry`：AI算力、半导体、电力能源、新能源、汽车、机器人、消费零售等。
- `code/name`：影响个股。
- `sentiment`：情绪分数，范围约为 `-1` 到 `1`。
- `impact_score`：事件影响分，范围 `0` 到 `100`。
- `ai_score`：历史 AI 分析里已有的个股分数。

## 5. Agent 评分

每只股票在某个日期由四个内部 Agent 打分：

- 新闻情绪 Agent：正负面关键词、历史 AI 分、新闻情绪强度。
- 事件影响 Agent：事件类型、行业、事件影响分、同类历史兑现表现。
- 技术走势 Agent：当时日 K 技术状态，包括 3日/5日/20日收益、量比、波动率、回撤。
- 风控 Agent：波动率、回撤、过热程度、负面事件。

最终买入分：

```text
buy_score =
  新闻情绪分 * sentiment_weight
  + 事件影响分 * event_weight
  + 技术走势分 * technical_weight
  + 风控可交易分 * risk_weight
```

## 6. 买入规则

静态推荐接口：

```text
GET /api/quant/recommendations
GET /api/quant/daily_plan
```

规则：

- `buy_score >= buy_threshold`：买入候选。
- `watch_threshold <= buy_score < buy_threshold`：重点观察。
- `buy_score < watch_threshold`：暂不买入。
- 如果 `sell_score >= avoid_sell_threshold` 且 `buy_score < avoid_buy_ceiling`：回避/卖出。

## 7. 全周期回放

回放接口：

```text
GET /api/quant/timeline
GET /api/quant/model/backtest
GET /api/front/trading_account
```

规则：

1. 按交易日从早到晚推进。
2. 当天只能看到当天及以前的新闻、历史样本和当时已有 K 线。
3. 当天新闻产生买入信号。
4. 信号不会当天成交。
5. 下一交易日按开盘价模拟买入。
6. 每天收盘后更新持仓、收益和卖出判断。
7. 用户账户从 `follow_start_date` 开始运行，不继承该日期以前的持仓。

## 8. 卖出规则

当前全周期回放和模拟持仓的卖出条件：

- 止损：持仓收益 `<= stop_loss_pct`。
- 止盈：持仓收益 `>= take_profit_pct`。
- 持仓到期：默认持有 `max_hold_days` 个交易日。
- 卖出评分触发：当日重新评分后 `sell_score >= sell_score_threshold`。

当前卖出价格使用卖出日收盘价；分时模式会优先使用分钟 K 触发价。

## 9. 分时逻辑现状

当前已经接入本地 5 分钟 K 缓存：

- 目录：`backend/data/kline_cache/*.csv`
- 格式：`time,open,close,high,low,volume,amount`
- 接口：`GET /api/quant/intraday_timeline`
- 必盈同步：`POST /api/data/biying/sync_intraday?date=YYYY-MM-DD&source=events&max_codes=200`

分时回放规则：

1. 新闻信号有明确时间戳。
2. 如果该股票当天有 5 分钟 K，买入价使用“信号时间之后下一根 5 分钟 K 的开盘价”。
3. 持仓期间逐根检查 5 分钟 K：
   - `low <= entry_price * (1 + stop_loss_pct / 100)`：按止损价卖出。
   - `high >= entry_price * (1 + take_profit_pct / 100)`：按止盈价卖出。
   - 如果同一根 5 分钟 K 同时触发止盈和止损，保守假设止损先发生。
4. 如果当天没有分钟 K，但开启 `use_daily_fallback=true`，则使用日线开盘/收盘价兜底。

## 10. 下一步

后续架构重点：

- 策略运行状态入库，当前已用 `strategy_runtime_snapshots` 缓存前台账户回放结果，并在 v0.2.23 新增 `strategy_daily_signals`、`strategy_runtime_positions`、`strategy_runtime_trades` 保存每个策略的每日信号、持仓和成交；v0.2.24 起 `strategy_runtime_snapshots` 同时保存 `daily_runtime:*` 正式每日账户快照，并新增 `strategy_runtime_settlements` 保存每日清算。
- 前台账户优先读取 `user_follow_snapshots`；未命中时读取 `strategy_runtime_trades`，再按用户跟随开始日和模拟资金派生账户；没有落库结果时才回退短缓存或即时回放。
- v0.2.52 起，前台策略页切换跟随策略不再强制刷新账户；保存后只重置跟随开始日，持仓/成交页再按需读取 `user_follow_snapshots`、`strategy_runtime_*` 或其它缓存。
- v0.2.53 起，前台账户默认不再把“没有落库结果”升级为同步即时回放；接口返回 pending，提示后台手动复盘或导入策略运行结果，避免用户请求拖住 API 进程。
- v0.2.54 起，pending 账户在前台会显示待生成提示，持仓、成交和交割空列表不再被解释成真实空仓。
- v0.2.55 起，前台账户日常路径不再把策略模型历史交割单作为同步兜底；模型交割单只用于策略库查看或受控排查，用户账户应来自策略运行表和用户跟随快照。
- v0.2.56 起，后台可手动执行前台账户预热，把已有策略运行结果提前派生为 `user_follow_snapshots`、`user_follow_positions` 和 `user_follow_trades`，让用户切换策略后的账户读取尽量命中已生成结果。
- v0.2.57 起，前台账户预热任务可转入独立 Python 子进程执行，批量用户账户派生不会长期占用 API 进程。
- v0.2.58 起，用户跟随周期变化会自动排队单用户账户预热，前台保存策略仍快速返回，账户结果由后台任务补齐。
- v0.2.59 起，前台保存策略时使用显式 `queued` 语义；v0.2.60 后“已排队”表示该用户已进入账户预热待处理队列。
- v0.2.60 起，账户自动预热使用待处理队列，避免同名预热任务运行中时丢掉后续用户的账户派生请求。
- v0.2.61 起，账户预热队列文件由跨进程锁保护，防止 API 写入和 worker 消费同时发生时覆盖彼此结果。
- v0.2.62 起，前台账户接口会发现并补启动残留账户预热队列的消费 worker，降低队列项滞留概率。
- v0.2.63 起，前台保存 profile 的热路径默认只把账户预热请求入队，不在保存请求里启动 worker；切换策略会更快返回，账户页读取仍会自愈唤起队列消费。
- v0.2.64 起，后台任务状态和“前台账户预热”任务卡片会展示账户预热队列摘要，能直接看到队列是否积压和锁是否残留。
- v0.2.65 起，后台手动“前台账户预热”会优先消费已排队用户，队列为空时才回到普通批量预热。
- v0.2.66 起，前台保存 profile 不再清空全局内存缓存，前台快照缓存键包含跟随开始时间，切换策略不会让数据覆盖率、策略列表等全局诊断缓存一起冷启动。
- v0.2.67 起，后台单模型回测重算不再同步等待 `walk_forward_intraday`，`recompute=true` 默认进入 `model_backtest` 后台任务并写入短缓存。
- v0.2.68 起，通用时间线回测接口不再默认同步等待 `walk_forward` 或 `walk_forward_intraday`，缓存未命中时进入 `quant_timeline` 后台任务。
- v0.2.69 起，前台切换策略默认只保存 profile 和跟随周期，并写入账户预热队列；资金档策略不再为保存动作加载完整策略模型目录，保存后也不自动拉取账户接口。
- v0.2.70 起，通用 `/api/quant/backtest` 不再默认同步等待 `quant_engine.backtest`；缓存未命中时进入 `quant_backtest` 独立进程，结果写入 SQLite 短缓存。
- v0.2.71 起，前台切换到策略库模型时，保存 profile 会按模型 ID 单条读取该策略参数，不再加载完整策略目录；用户切进化策略和切资金档策略都走轻量链路。
- v0.2.72 起，后台参数拟合 `/api/quant/fit_strategy` 不再默认同步等待多组参数 walk-forward；请求会进入 `fit_strategy` 独立进程，只有 `defer=false` 才同步执行。
- v0.2.73 起，分时行情同步不再默认在 HTTP 请求内直接调用必盈抓取；后台按钮和 `/api/data/biying/sync_intraday` 都进入 `market_sync` 独立进程。
- v0.2.74 起，日K补齐和龙虎榜同步也不再默认在 HTTP 请求线程内执行；后台按钮进入 `kline_fill`、`lhb_sync` 独立进程。
- v0.2.75 起，日常模拟交易循环也默认进入 `trade_cycle` 独立进程；自动调度只负责启动任务和记录状态。
- v0.2.76 起，新闻 AI 结构化分析默认进入 `ai_analysis` 独立进程；自动调度只负责启动任务和记录状态。
- v0.2.77 起，数据覆盖率诊断结果写入 SQLite 短缓存；缓存未命中时进入 `data_coverage` 独立进程，后台和前台状态页只读缓存或 pending。
- v0.2.78 起，新闻抓取默认进入 `news_fetch` 独立进程；自动调度只负责启动任务和记录状态。
- v0.2.79 起，系统启动流程整体默认进入 `system_startup` 独立进程；API 进程只投递任务，子进程内按顺序完成新闻、AI、补数、行情和交易，不自动执行策略复盘、训练或回测。
- v0.2.80 起，前台 profile 保存使用轻量资金档目录时不再查询 `strategy_runtime_*` 摘要；切换策略只保留定位当前策略所需参数。
- v0.2.81 起，前台保存策略后不再在概览页立即补拉推荐和日计划，避免一次策略切换连带触发额外缓存未命中任务。
- v0.2.82 起，前台首屏快照默认只带轻量策略上下文，不再提前加载完整策略库；策略库列表由策略页按需请求。
- v0.2.83 起，通用回测和模型重新回测增加显式手动触发门槛：缓存未命中且没有 `manual=true` 时不排后台任务也不同步重算，只返回 `manual_required`。
- v0.2.84 起，训练、策略复盘、回测和参数拟合类独立进程默认同一时间只允许运行一个；超过 `QT_HEAVY_JOB_MAX_CONCURRENT` 时新请求返回 `busy`，不再叠加启动。
- v0.2.85 起，单模型回测重算和时间线回测也从 API 进程内后台线程迁移到独立 Python 子进程，减少重放计算对 Web 请求的影响。
- v0.2.86 起，后台界面展示重任务并发槽位和正在运行的重任务，帮助运维判断是否应等待、停止任务或稍后再启动新的复盘/训练/回测。
- v0.2.87 起，单模型回测和时间线回测不再依赖进程内内存缓存，子进程会把结果写入 SQLite 短缓存，父进程刷新后能读取到完成结果。
- v0.2.88 起，用户切换策略后的跟随周期落库默认异步执行，避免后台复盘/回测写库时拖慢 profile 保存；重任务并发满时接口直接返回 `busy`，不再显示成“后台生成中”。
- v0.2.89 起，用户切换策略后的账户预热入队默认异步执行；即使账户预热队列文件被独立 worker 锁住，前台保存 profile 也不再等待该锁。
- v0.2.90 起，账户页发现当前用户账户运行结果仍为 pending 时，会异步补账户预热队列并启动 worker，作为 profile 异步入队之外的自愈路径。
- v0.2.91 起，前台会把账户预热排队/启动状态展示到 pending 文案里，用户可以区分“正在排队预热”和“需要后台手动生成运行结果”。
- v0.2.92 起，账户预热异步入队有短时间去重；频繁刷新 pending 账户不会为同一个用户重复创建多个队列写入线程。
- v0.2.93 起，登录首屏 snapshot 返回 pending 账户时同样会触发账户预热自愈，避免前端因为已有 pending account 对象而不再请求账户接口。
- v0.2.94 起，账户 pending 文案会提示重复预热请求已被去重，解释频繁刷新时为什么不重复排队。
- v0.2.95 起，后台任务状态和运维页可看到账户预热异步去重摘要，帮助判断 pending 账户是否已经触发自愈且被去重保护。
- v0.2.96 起，切换策略的 profile 保存接口返回阶段耗时诊断，便于区分慢在策略模型上下文读取、跟随周期记录还是账户预热入队。
- v0.2.97 起，profile 保存不会在轻量模式下因为缺失策略 ID 回退全量策略库；过期 ID 会被替换为当前资金规模的推荐资金档策略。
- v0.2.98 起，profile 保存链路复用更新后的用户资料构建策略上下文，减少一次认证数据文件读取和锁等待。
- v0.2.105 起，`load_runtime_account` 和 `runtime_model_summaries` 读取正式每日运行快照时不再依赖 `LIKE 'daily_runtime%'`，改用 source 前缀范围过滤，避免非 daily runtime 缓存混入策略运行摘要。
- v0.2.104 起，用户当前跟随周期的读取和关闭旧周期路径去掉 `COALESCE(ended_at, '')` 包列条件，改用 NULL/空串显式判断并补索引，降低切换策略时写入跟随周期的 SQLite 扫描风险。
- v0.2.103 起，策略信号读取不再为了判断最新日期执行全表计数；没有指定日期时取最新一条信号日期，指定日期时取不晚于该日期的最新信号日，找不到且关闭 fallback 时返回空结果。
- v0.2.102 起，策略演进数据库连接不再每次调用都重复执行 schema 初始化脚本；同一进程、同一数据库路径、同一 schema 版本只初始化一次，减少账户和策略读取接口的固定开销。
- v0.2.101 起，`strategy_runtime_*` 和 `user_follow_*` 表补充热路径复合索引，并把用户诊断里的资金匹配从 `ABS(initial_cash - ?)` 改为范围条件，避免索引失效。
- v0.2.100 起，profile 保存前的策略目录整理和策略 ID 解析逻辑迁入 `app.quant.front_profile`，用独立单测固定热路径规则，减少 `main.py` 继续膨胀。
- v0.2.99 起，profile 保存链路把策略 ID 纠正提前到保存前，并复用保存前解析到的模型，避免先保存过期策略再由上下文纠正二次写入或重复查模型。
- 前台推荐和日计划已先用 `frontend_payload_cache` 做短缓存；v0.2.129 起缓存未命中默认只返回 pending/disabled，不自动触发 `frontend_payload_precompute`，不再由普通用户访问启动批量计算。
- 后台数据库管理页可以查看并清理上述缓存，便于排查服务器接口变慢或缓存失效问题。
- v0.2.25 起，后台慢任务触发接口默认快速返回，实际新闻、AI、行情、补数、交易循环和策略复盘继续在任务线程里运行，避免反向代理等待完整计算。
- v0.2.28 起，服务器自动策略复盘按日期窗口分批推进，默认 15 天一批，避免常规调度每小时全量重跑历史区间。
- v0.2.30 起，手动策略复盘和策略进化可以由独立 Python 子进程执行，降低重计算对 API 进程的影响。
- v0.2.31 起，独立进程任务状态会自动巡检，异常退出时标记失败，避免任务状态永久停留在运行中。
- v0.2.32 起，用户跟随账户快照、持仓和成交入库，避免每次页面打开都重复缩放、裁剪和派生同一周期账户。
- v0.2.33 起，用户跟随周期和后台账户诊断入库/展示，资金变化与策略切换一样会开启新周期。
- v0.2.49 起，策略复盘、模型训练和回测默认只手动触发；策略库查看交割单默认读取已保存模型记录；日常自动链路使用已训练模型，主要执行新闻抓取、AI 分析、行情同步、模拟交易和前台缓存预计算。
- 高频状态用 WebSocket 或增量轮询，重接口拆成后台任务加进度查询。
- 推荐、日计划、数据覆盖率、单模型回测重算和通用时间线回测默认走轻量缓存和后台任务；剩余慢接口后续也应改为后台任务 + 进度 + 缓存读取。
## v0.2.106 日常运行边界

- 普通交易日不应自动训练或全量回测模型；系统使用已训练模型，结合当天新闻、AI 分析、行情和持仓状态生成买卖信号。
- 策略复盘、训练、回测和参数拟合属于手动维护动作。前台用户切换策略只更新跟随关系和入队账户预热，不触发这些重计算。
- 前台账户读取优先使用 `user_follow_*` 和 `strategy_runtime_*` 已落库结果；缺失时返回 pending 或由后台任务补齐。

## v0.2.107 非交易计算隔离

- 访问审计批量落盘属于系统运维优化，不参与买卖评分、回放、持仓或账户收益计算。
- 用户切换策略后的账户结果仍只由跟随关系、策略运行表和用户账户快照决定，不会受访问日志写入节奏影响。

## v0.2.108 用户资料热路径

- `auth.json` 缓存只影响读取用户 profile 和认证配置的 I/O 开销，不改变策略选择、跟随开始日、资金规模或账户派生规则。
- 切换策略仍会重置 `follow_started_at` / `follow_start_date`，账户视图继续从新的跟随开始日读取或派生。

## v0.2.109 前台鉴权热路径

- 前台用户切换策略、读取账户和读取快照时，frontend scope 鉴权不再构建完整后台认证状态；这只减少请求固定开销，不改变任何买卖评分、回放或账户计算规则。
- 后台 admin scope 仍保留初始化检查，训练、复盘和回测的手动触发边界不变。

## v0.2.110 前台快照热路径

- 前台快照不再参与后台运行日志读取，日志只用于后台运维诊断，不参与买卖评分、持仓、成交或账户收益计算。
- 轻量任务状态短缓存只影响页面显示的运行状态新鲜度，不改变策略信号、交易结果或用户跟随账户视图。

## v0.2.111 新闻 SQL 热路径

- 新闻最新时间、新闻列表和新闻事件列表的排序改为索引友好的字段排序，减少大表扫描后的临时排序。
- 该调整只影响公共新闻数据读取效率，不改变新闻事件内容、评分权重、买卖规则或账户派生逻辑。

## v0.2.112 前台状态读取边界

- 前台状态里的最新新闻时间使用短缓存复用 5 秒内的查询结果，降低连续快照请求对 SQLite 的重复读取。
- 这只是展示层新鲜度信息缓存，不改变新闻入库内容、AI 分析、策略信号、交易模拟或用户账户收益计算。

## v0.2.113 前台日期边界

- 前台默认 `as_of` 和用户跟随起止裁剪使用 SQLite 日期边界短缓存获取，不再默认通过完整事件列表推导最新交易/事件日期。
- 日期边界只用于选择展示和账户读取窗口；具体买入、卖出、成交、持仓和收益仍由策略运行表或账户派生逻辑决定。

## v0.2.114 账户派生写入职责

- 轻量快照读取账户时只返回可用结果或 pending 状态，不在请求内同步保存用户跟随账户派生表。
- 账户派生写入仍遵守原有计算逻辑，但应由账户预热、显式账户接口或后台维护任务承担，减少首屏请求对 SQLite 写锁的依赖。

## v0.2.115 前台账户 GET 只读

- 前台账户 GET 默认不保存派生账户结果；如果运行表或缓存已经能给出账户视图，接口直接返回，并把需要落库的用户跟随账户交给后台预热队列。
- 这不改变账户收益、持仓、成交或跟随开始日计算，只改变派生结果写入时机。

## v0.2.116 策略运行矩阵只读观测

- `/api/admin/strategy_runtime/matrix` 只汇总已落库的策略运行状态和最新信号，用于判断某个策略是否已经具备可派生用户账户的每日结果。
- 该矩阵不参与买入评分、卖出评分、仓位计算、成交模拟或用户账户收益计算，也不会生成新的策略信号。
- 如果矩阵显示策略缺少运行数据，应通过后台手动策略复盘或运行结果导入补齐；日常前台切换策略仍只更新跟随关系和账户预热队列。

## v0.2.117 策略运行矩阵页面不改变量化逻辑

- 后台页面展示的策略运行矩阵只消费已有 `strategy_runtime_*` 和 `strategy_daily_signals` 汇总，不改变任何买卖规则、评分权重、成交价格、持仓更新或账户派生规则。
- 页面中的“缺运行结果”只表示该策略缺少可读取的每日运行表结果，不代表策略收益为 0、真实空仓或信号为空。

## v0.2.118 策略运行矩阵服务不参与计算

- `app.quant.strategy_runtime_matrix` 只负责把已有策略运行汇总和信号 feed 归一化为后台展示 payload，不参与策略评分、交易回放、训练、回测或账户派生。
- 该服务的单测只固定展示语义和字段合并规则，不作为收益、胜率或成交正确性的证明；量化计算仍以 `strategy_runtime_*` 生成链路和账户测试为准。

## v0.2.119 路由拆分不改变量化语义

- `/api/admin/strategy_runtime/matrix` 迁到独立 router 后，仍调用同一套 payload 组装服务和同一批策略运行汇总数据。
- 该调整只改变代码组织，不改变策略信号、成交、持仓、账户收益或用户跟随裁剪逻辑。

## v0.2.120 策略账户路由拆分不改变量化语义

- `/api/admin/trading_account` 和 `/api/admin/strategy_runtime/replay` 迁到独立 router 后，仍调用原 `_admin_strategy_trading_account` / `_admin_strategy_runtime_replay` 逻辑。
- 后台展示账户仍来自已落库策略运行结果和原有派生逻辑，不改变买卖评分、成交模拟、资金流水或用户前台账户计算。

## v0.2.121 前台快照快路径不改变量化语义

- 轻量快照读取 `strategy_runtime_snapshots.account_json` 只是复用策略复盘已经落库的每日账户结果，不重新生成买卖信号，也不改变成交价格、仓位规则或收益计算。
- `hydrate_trades=false` 只用于首屏/概览快照的展示加速；账户页和后台策略账户仍可按需读取 `strategy_runtime_trades` 生成完整历史成交、交割单和资金流水。
- 切换策略时不启动账户预热 worker 只改变任务启动时机，不改变用户 `follow_start_date` 裁剪、模拟资金缩放或后续用户账户派生规则。

## v0.2.122 前台 profile router 不改变量化语义

- `/api/front/profile` 迁入独立 router 只改变代码组织，不改变 profile 字段、资金档推荐、策略 ID 解析、跟随开始日重置或账户预热排队规则。
- 新 router 不参与买卖评分、信号生成、交易撮合、持仓计算、训练、复盘或回测；量化语义仍由原策略、账户和运行表逻辑决定。

## v0.2.123 前台运行 router 不改变量化语义

- 前台 public snapshot、登录 snapshot、策略目录和交易账户迁入 `app.routers.frontend_runtime` 后，仍调用原 payload 逻辑读取新闻、策略上下文、运行快照和用户账户。
- 路由拆分不改变 `strategy_runtime_snapshots` 快路径、`user_follow_*` 优先级、账户 pending 语义、买卖信号、成交模拟、训练、复盘或回测触发边界。

## v0.2.124 前台买入 router 不改变量化语义

- 前台推荐和日计划迁入 `app.routers.frontend_signal` 后，仍调用原 payload 逻辑读取用户策略上下文、短缓存和预计算任务结果。
- 路由拆分不改变新闻评分、买入候选、日计划生成、可买一手约束、缓存未命中 pending 语义或后台预计算触发边界。

## v0.2.125 后台概览 router 不改变量化语义

- 后台概览和模型信号迁入 `app.routers.admin_overview` 后，仍调用原 payload 逻辑读取策略目录、信号 feed、任务状态和账户摘要。
- 路由拆分不改变买卖评分、信号生成、成交模拟、账户收益、策略复盘、训练或回测触发边界。

## v0.2.126 后台数据库与缓存 router 不改变量化语义

- 后台数据库和缓存接口迁入 `app.routers.admin_data_cache` 后，仍调用原 payload 逻辑查看 SQLite 表、分页读取表数据、查看缓存状态和清理缓存。
- 路由拆分不改变策略信号、推荐、日计划、成交、持仓、账户收益、训练、复盘或回测计算规则。

## v0.2.127 后台访问审计 router 不改变量化语义

- 后台访问日志和访问安全接口迁入 `app.routers.admin_access` 后，仍调用原 payload 逻辑读取审计日志、识别异常访问和维护封禁 IP。
- 路由拆分不改变任何新闻评分、策略信号、推荐、日计划、成交、持仓、账户收益、训练、复盘或回测计算规则。

## v0.2.128 后台用户管理 router 不改变量化语义

- 后台前台用户管理接口迁入 `app.routers.admin_frontend_users` 后，仍调用原 payload 逻辑创建用户、更新 profile、记录跟随周期、封禁/解封和删除用户。
- 路由拆分不改变用户模拟资金、跟随策略、跟随开始日、账户裁剪、买卖信号、成交、持仓、训练、复盘或回测计算规则。

## v0.2.129 前台预计算不改变买卖逻辑

- 前台推荐和日计划缓存未命中时默认不再自动启动批量预计算，只返回待生成状态；这只改变任务触发时机，不改变推荐评分、日计划生成、可买一手过滤或策略参数。
- 后台手动前台缓存预计算默认 `force=false`，已有未过期缓存会跳过；需要重算时由管理员显式传入强制参数。
- 推荐缓存默认 TTL 改为 1800 秒，与日计划 TTL 对齐，减少用户访问触发重复补算的概率。

## v0.2.130 前台预计算可观测不改变量化语义

- `frontend_payload_policy` 只把前台预计算开关、TTL、用户上限和进程模式暴露给后台状态，不参与推荐评分、日计划生成、成交模拟或账户收益计算。
- 后台调度器文案按该 policy 显示“手动/自动”，只改变运维可见性，不改变任何买卖信号或用户账户裁剪逻辑。

## v0.2.131 任务运维 router 不改变量化语义

- 任务状态、日志、调度器控制和任务暂停/恢复/停止迁入 `app.routers.admin_jobs` 后，仍调用同一套 `_jobs_status_payload` 和 `job_manager` 方法。
- 前台快照任务摘要改为只读调度器、running 和 paused 状态，不触发完整后台状态巡检；这只减少前台状态查询开销，不改变任何账户或信号计算。
- 该调整只改变代码组织和前台状态读取路径，不改变新闻评分、策略信号、成交撮合、账户收益、训练、复盘、回测或前台预计算的计算规则。

## v0.2.132 任务执行 router 不改变量化语义

- 具体任务启动入口迁入 `app.routers.admin_job_runs` 后，仍调用原 `job_manager` 方法和原系统启动流程 payload。
- 路由拆分不改变新闻抓取、AI 结构化、行情同步、策略复盘、交易循环、前台预计算或账户预热的执行逻辑，也不改变默认手动/自动边界。

## v0.2.133 核心系统 router 不改变量化语义

- 版本、认证、调试、配置和 `/api/status` 迁入 `app.routers.core_system` 后，仍调用原 payload 回调。
- `/api/status` 只减少任务摘要读取开销，前台快照新闻短缓存只复用已读取的新闻摘要；这些都不改变新闻事件、策略信号、交易撮合、账户收益、训练、复盘或回测逻辑。
## v0.2.134 前台预计算限流不改变量化语义

- 前台推荐/日计划预计算新增批量上限和时间预算，只改变缓存生成节奏，不改变推荐评分、日计划生成、可买一手过滤、策略参数或账户收益计算。
- 轻量前台快照在 SQLite 新闻或日期缺失时返回轻量占位，不回退完整引擎加载；这只影响页面冷启动耗时和展示新鲜度，不参与买卖信号、成交、持仓或用户账户派生。
- 账户预热异步入队 worker 池只改变排队调度方式，不改变用户 `follow_start_date` 裁剪、模拟资金缩放或 `strategy_runtime_*` 读取优先级。
